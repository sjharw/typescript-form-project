// check that script has been accessed
let indexmessage: string = 'index script present';
console.log(indexmessage);

// log input from form to the console
import { FormInput } from './inputForm';
const myVariable = new FormInput.InputForm;
console.log(myVariable);